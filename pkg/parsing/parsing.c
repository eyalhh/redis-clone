#include "parsing.h"

parsed_t *parse(char *cmd) {

    int cap = 50;
    parsed_t *res = calloc(1, sizeof(parsed_t));
    res->flags = calloc(cap, sizeof(flag_t));



    

    




    return res;

}

